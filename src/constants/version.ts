/**
 * Shwe Let Yar - Application Version & Format Constants
 * Single authoritative source of version definitions.
 */

export const APP_VERSION = '2.5.0';
export const CURRENT_APP_VERSION = APP_VERSION;

export const BACKUP_FORMAT_VERSION = '3.0';
export const CURRENT_BACKUP_FORMAT_VERSION = BACKUP_FORMAT_VERSION;

export const DATABASE_SCHEMA_VERSION = 3;
export const CURRENT_DATABASE_SCHEMA_VERSION = DATABASE_SCHEMA_VERSION;
